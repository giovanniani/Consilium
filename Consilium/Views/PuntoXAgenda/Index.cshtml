@model IEnumerable<Consilium.Models.PuntoXAgenda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Agenda.idAgenda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Punto.titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Agenda.idAgenda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Punto.titulo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPuntoXAgenda }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPuntoXAgenda }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPuntoXAgenda })
        </td>
    </tr>
}

</table>
